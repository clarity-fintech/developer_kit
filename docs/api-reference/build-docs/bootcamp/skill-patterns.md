# Bootcamp — Skill patterns

Quantum Skills (macro) + Nano-Skills (micro) under dual-lock.

```bash
clrty strategy run --steps="cortexpay-predict,helix-intent-resolve,helix-net-settle"
clrty strategy run --steps="nt-optimize,helix-intent-resolve,attestation-verify"
```

Manifests: `quant_skills_manifest.json`, `nano_skills_manifest.json`
