# Nano-skills recipes (N01–N20)

```bash
clrty nano list
clrty nano run N05 --dry-run
clrty nano evidence --catalog=N05
```

Compose into macro pipelines:

```bash
clrty strategy run --steps="N02,N05,N09,metric-collapse-arbitrage"
```

Catalog manifest: `CLRTY_SUBSTRATE/boot/nano_skills_manifest.json`

**Namespace:** `nano:N01` — not VIS infrastructure `VIS-N01`.
