# Sets recipes (CCR 99→1)

```bash
clrty id profile 0xYourWallet
clrty token layout-v2
clrty nano run N04 --dry-run   # set-collapse-estimator
```

Tier migration uses deterministic heuristics in v1 — full Monte Carlo deferred to Phase 2.
